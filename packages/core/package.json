{
  "name": "@miniclaw/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interfaces and shared types for miniclaw. Implementations depend on this; this depends on nothing miniclaw-specific.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  }
}
