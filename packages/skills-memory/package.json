{
  "name": "@miniclaw/skills-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "write_memory and search_memory skills. Backend-agnostic — uses MemoryStore from core.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@miniclaw/memory-sqlite": "workspace:*"
  }
}
