{
  "name": "@miniclaw/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tool-using agent loop. Depends on @miniclaw/core ONLY — composes via interfaces.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*"
  },
  "devDependencies": {
    "@miniclaw/agent-skills": "workspace:*",
    "@miniclaw/context-windowed": "workspace:*",
    "@miniclaw/memory-sqlite": "workspace:*",
    "better-sqlite3": "^12.10.0",
    "zod": "^3.23.8"
  }
}
