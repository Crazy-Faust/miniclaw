{
  "name": "@miniclaw/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Long-running daemon that supervises sessions. Wraps the agent in a SessionRegistry keyed by channel, exposes a Unix-socket attach protocol, and ticks the cron scheduler.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/agent": "workspace:*",
    "@miniclaw/core": "workspace:*",
    "@miniclaw/harness": "workspace:*"
  }
}
