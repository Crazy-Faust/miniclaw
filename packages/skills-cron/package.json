{
  "name": "@miniclaw/skills-cron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "cron_add, cron_list, cron_remove, cron_pause skills. The gateway's CronScheduler is the runtime.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*",
    "@miniclaw/gateway": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@miniclaw/memory-inmemory": "workspace:*"
  }
}
