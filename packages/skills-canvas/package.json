{
  "name": "@miniclaw/skills-canvas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "canvas_create / canvas_update / canvas_list / canvas_delete skills, plus an HTTP handler that renders the canvas as static HTML for the user to open in a browser.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*",
    "zod": "^3.23.8"
  }
}
