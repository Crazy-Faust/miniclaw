{
  "name": "@miniclaw/context-stateless",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Stateless ContextManager — no history, no retrieval. Just system + new user message. Useful for one-shot runs and as a baseline.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*"
  }
}
