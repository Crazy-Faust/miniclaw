{
  "name": "@miniclaw/skills-db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only SQL skill. Owns its own SQL guard.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*",
    "better-sqlite3": "^12.10.0",
    "zod": "^3.23.8"
  }
}
