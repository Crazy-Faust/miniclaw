{
  "name": "@miniclaw/skills-todo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "todo_write skill + in-session TodoStore + /todos viewer meta-command. Lets the model maintain a multi-step plan across turns.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@miniclaw/core": "workspace:*",
    "zod": "^3.23.8"
  }
}
